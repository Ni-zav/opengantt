# Contributing

1. Open an issue before large product or schema changes.
2. Create a focused branch and preserve the invariants in `AGENTS.md`.
3. Run `npm test` and `npm run build`.
4. Include a migration for database changes and explain compatibility effects.
5. Keep pull requests small enough to review and avoid speculative abstractions.

Contributions are accepted under AGPL-3.0-only.
